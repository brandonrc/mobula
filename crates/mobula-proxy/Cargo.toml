[package]
name = "mobula-proxy"
description = "Identity-aware proxy for Ray dashboards and Serve endpoints (standalone mode)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[features]
# Forwards mobula-auth's defaults: its workspace edge is
# default-features = false so members can select feature sets (#61).
default = ["mobula-auth/default"]

[dependencies]
mobula-auth.workspace = true
axum.workspace = true
reqwest.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
jsonwebtoken.workspace = true
serde_json.workspace = true
tower = { workspace = true }
rsa = { version = "0.9", features = ["pem"] }
rand = "0.8"
