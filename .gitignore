target/
*.log
.DS_Store

# Local dev-stack scratch (SQLite store, smoke logs) — scripts/dev-stack.sh
.dev/

# Generated OpenAPI spec — emitted by the export_openapi test and pushed to
# the mobula-api repo by CI; never committed here (source of truth is the code).
openapi.json

# local serve pid file
server.pid
